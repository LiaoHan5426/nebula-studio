/**
 * Re-export shared chrome catalog from `@nebula-studio/app-shell`
 * (Web + Electron main must share the same offline snapshot).
 */
export {
  SHELL_CHROME_CATALOG,
  SHELL_INTEGRABLE_DISPLAY_ORDER,
} from '@nebula-studio/app-shell';
