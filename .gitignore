# Logs (repo root only; do not use bare `logs` — it ignores e.g. settings/features/logs/)
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.temp
playwright-report/
test-results/
# Module Federation Vite plugin cache (diagnostics/latest.json has a per-run timestamp)
**/.mf/
# Electron app build artifacts (electron-vite → `out/`, electron-builder → `dist/` under package)
apps/electron/out/
apps/electron/dist/
apps/electron/public/logs/

node_modules
# Editor directories and files
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ESLint
.eslintcache

# AI agent worktrees
.claude/worktrees/
.cursor
.rtk
.trae
.qoder

