# Shared defaults (all modes). Mode-specific files override these.
# Local overrides: .env.local / .env.[mode].local (gitignored).

NEBULA_FEDERATION_DEV_HOST=127.0.0.1
NEBULA_FEDERATION_REMOTE_CACHE_DIR=node_modules/.vite/federation-remotes
