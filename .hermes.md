# Nebula Studio Hermes 项目入口

## 开始任何任务前

1. 读取本仓库根目录 `AGENTS.md` 和任务相关的仓库文档；工具链统一遵循仓库的 Vite+ / `vp` 规则。
2. 定位工作区知识库，按顺序使用第一个存在的目录：
   - 环境变量 `WIKI_PATH`；
   - 工作区相对路径 `../.knowledge/agent-memory`。
3. 读取知识库中的：
   - `projects/LiaoHan5426--nebula-studio/index.md`
   - `projects/LiaoHan5426--nebula-studio/workspace-map.md`
   - 涉及后端 API、认证、租户、SSE 或真实栈时再读 `projects/nebula-ecosystem.md` 与 Nebula 项目首页。
4. 用知识库的 `scripts/project_navigation.py check --workspace-root ..` 核对叙述页面是否绑定当前 HEAD；过期内容只能作为导航，结论必须回到当前源码核验。

## 开发导航

- Workspace package、脚本和内部依赖定位：知识库 `workspace-map.md` / `workspace-manifest.yaml`。
- 调用、依赖和变更影响：优先使用工作区虚拟环境中的 `code-review-graph`：
  - Windows：`../.venv/Scripts/code-review-graph.exe`
  - Linux/macOS：`../.venv/bin/code-review-graph`
- 代码图不可用或状态过期时，先运行 `update --skip-flows`；仍不可用则回退到 Git 跟踪文件和源码静态检查，并明确说明未执行结构化影响分析。
- 只把 Git 跟踪的 Markdown/YAML 作为权威知识；代码图、向量索引和缓存均是本机可重建状态。

## 可移植性

不得把盘符、用户名或某台机器的绝对路径写入项目配置。完整 Nebula Workspace 应由控制仓库的 `workspace.ps1 init` 或 `workspace.sh init` 构建。
